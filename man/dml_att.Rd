% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dml_att.R
\name{dml_att}
\alias{dml_att}
\title{DML estimator for the Average Treatment effect on the Treated (ATT)}
\usage{
dml_att(
  X,
  A,
  Y,
  K = 5,
  regularization = 0.1,
  stratified = TRUE,
  seed = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{X}{Data.frame or matrix of covariates. Must be binary (0/1) for treefarmr.}

\item{A}{Integer or numeric vector of treatment (0/1).}

\item{Y}{Integer or numeric vector of outcome (0/1). Binary only; continuous Y not supported.}

\item{K}{Number of cross-fitting folds. Default 5.}

\item{regularization}{Numeric. Tree complexity penalty passed to treefarmr. Default 0.1.}

\item{stratified}{Logical. If TRUE (default), fold assignment is stratified by A.}

\item{seed}{Optional. Random seed for fold creation.}

\item{verbose}{Logical. Passed to treefarmr. Default FALSE.}

\item{...}{Additional arguments passed to treefarmr::fit_tree.}
}
\value{
List with elements: theta (point estimate), sigma (estimated SE), ci_95 (Wald 95% CI),
  score_values (influence at theta), nuisance_fits (per-fold models), fold_indices, n, K.
}
\description{
Estimates the ATT using double machine learning with optimal decision trees
(via treefarmr) for the nuisance functions e(X), m0(X), m1(X). Uses log-loss
only; binary outcome Y required. Continuous outcome is not implemented yet.
}
\references{
Manuscript equation (2) for the orthogonal score; Chernozhukov et al. for DML.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_att_msplit.R
\name{discover_modal_structures}
\alias{discover_modal_structures}
\title{Discover modal nuisance structures across M sample splits (M-split Stage 1)}
\usage{
discover_modal_structures(
  X,
  A,
  Y,
  M,
  K,
  seed_base = NULL,
  outcome_type = "binary",
  verbose = FALSE
)
}
\arguments{
\item{X, A, Y}{Covariates, treatment (0/1), outcome.}

\item{M, K}{Number of splits and folds per split.}

\item{seed_base}{Base seed; split m uses \code{seed_base + m} (NULL = no seed).}

\item{outcome_type}{"binary" or "continuous" (selects the outcome loss).}

\item{verbose}{Logical; print per-split progress.}
}
\value{
List with \code{e} and \code{m0}, each a \code{select_structure_modal}
  result (\code{structure}, \code{discretization_metadata}, \code{frequency}, ...).
}
\description{
Shared Stage-1 of the two M-split estimators (\code{estimate_att_msplit} and
\code{estimate_att_msplit_averaged}). For each of \code{M} splits: create
stratified folds (seed \code{seed_base + m}), fit propensity (log_loss) and
control-outcome nuisances on fold-1's training set via adaptive-CV lambda
selection (capped at 15x the theory rate to avoid stump-producing lambda, depth
bounded at 4L to match the Rashomon path), extract the tree structures, then pick
the modal structure per nuisance excluding stumps (\code{min_leaves = 2L}).
}
\keyword{internal}

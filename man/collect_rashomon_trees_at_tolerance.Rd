% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_att_averaged.R
\name{collect_rashomon_trees_at_tolerance}
\alias{collect_rashomon_trees_at_tolerance}
\title{Collect Rashomon Trees at Given Tolerance with CV-Selected Lambda}
\usage{
collect_rashomon_trees_at_tolerance(
  X,
  outcome,
  K,
  fold_indices,
  tolerance,
  regularization,
  outcome_type,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{X}{Data.frame of covariates}

\item{outcome}{Numeric vector (A for propensity, Y[A==0] for outcome)}

\item{K}{Number of folds}

\item{fold_indices}{List of length K with test indices per fold}

\item{tolerance}{Numeric. Rashomon bound multiplier (epsilon_n)}

\item{regularization}{Numeric. Not used (CV selects lambda). Kept for backward compatibility.}

\item{outcome_type}{Character. "binary" or "continuous"}

\item{verbose}{Logical}

\item{...}{Additional arguments for optimaltrees}
}
\value{
List with:
  \item{all_trees}{List of all tree structures (nested lists) from all K folds}
  \item{all_structures}{List of all TreeStructure objects corresponding to all_trees}
  \item{intersection_trees}{List of trees in intersection (could be empty)}
  \item{n_total}{Total trees collected across all K folds}
  \item{n_intersecting}{Number of trees in intersection (0 if empty)}
  \item{tolerance_used}{The tolerance value used}
}
\description{
Fits K Rashomon sets (one per fold) at specified tolerance and collects
all trees from all folds. Uses CV to select lambda for each fold.
}
\keyword{internal}

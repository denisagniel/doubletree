# Export public API only (internal helpers are in R/*.R with @noRd)
export(create_folds)
export(psi_att)
export(att_se)
export(att_ci)
export(estimate_att)
export(estimate_att_single_tree)
export(att_repeated)

# M-split functionality
export(estimate_att_msplit)
# NOTE: select_structure_modal, analyze_structure_diversity,
# compute_functional_consistency, average_trees, and the averaged-tree predictor
# were relocated to optimaltrees (2026-07-08); call them as optimaltrees::<fn>.

# Averaged-tree estimators
export(estimate_att_doubletree_averaged)
export(estimate_att_msplit_averaged)

# S3 methods for msplit_att
S3method(print, msplit_att)
S3method(summary, msplit_att)
S3method(plot, msplit_att)

# Session Notes — 2026-09-21

## 09:05 — discussion-completion-and-section2-diff-review

**Goal.** Resume paragraph-by-paragraph manuscript drafting. Initial read of `outline.md`/
`story.md`/session notes suggested the Discussion (§5) owed three items from the 2026-09-18
session (`prop:spectest`'s unvalidated size/power caveat, narrowing the audit-trail-availability
claim, and a new ATT-vs-ATE scope remark) — drafted and audited those. Author then clarified
"where we left off" meant the Introduction was complete and the next stop was Section 2, not
Discussion; pivoted to a git-diff-based review of what Section 2 actually contains as of last
week's commit.

**What happened.**

1. **Discussion (§5) completion** (kept as a separate, self-contained edit per author's
   direction, not reverted): drafted the three owed items, then commissioned `domain-reviewer`
   (session `ses_f3bafbc32ffeJ3KT6f13vDo4u3`) and `proofreader` (session
   `ses_f3bafbbb6ffex4obMlBarDmKzD`) audits against the first draft.
   - `domain-reviewer` found real defects: the finite-sample caveat omitted `prop:spectest`'s
     dependence on the unverifiable `ass:rate`; framing the diagnostic as licensing "which
     interval to report" implied a coverage guarantee the proposition does not establish (the
     post-selection-inference hazard `sec:lasso-comparison` itself cites `leeb2005` against); the
     closing sentence was self-undercutting (claimed the interval is audit-complete "when
     structural sparsity holds," but the diagnostic's own test statistic is built from the
     anchor, so running it pulls the anchor in regardless of sparsity's truth); a modal mismatch
     on "the anchor... may use $K$-fold cross-fitting... inherits *exactly* the cost
     cross-fitting imposes"; and a citation-fidelity problem in the new paragraph — the
     single-tree corollary family was said to "rest on" `hahn1998`, but `cor:single-tree` is a
     pure algebraic identity (Hahn explains the family's *interest*, not its premises), and
     `rem:two-trees-why`'s case for a propensity tree was wrongly attributed to Hahn wholesale
     when only its second of two explicitly separated points does.
   - `proofreader` found terminology drift ("bound" vs. the manuscript's established "width"),
     an ambiguous adverb placement, a non-standard "single-outcome-tree" term where the rest of
     the manuscript says "single-tree," and several punctuation/density issues.
   - Fixed all of the above; re-verified label/`\ref` integrity (44 labels, 35 refs, zero
     missing) after every edit. Updated `claims.md` (`prop:spectest` row: notes the Discussion
     now discloses the GAP; ATT-vs-ATE scope row: status → Drafted, softened to match the
     prose's explicit non-claim on generalization, Hahn attribution corrected, "the CLT" dropped
     from the machinery list to reconcile the registry onto the more defensible prose) and
     `outline.md` §5's Discussion row. Full rationale in
     `quality_reports/reviews/2026-09-21_discussion-completion.md` (new file, new directory —
     `quality_reports/reviews/` did not previously exist in this project).
   - **Correction surfaced while updating the outline:** item (iv) (trees-vs-linear
     representational trade-off paragraph) was listed in `outline.md` as "not yet drafted," but
     was in fact already drafted in the 2026-09-18 commit alongside `rem:saturated-general` —
     fixed the outline's listing, not the manuscript.

2. **Section 2 status review**, prompted by the author's correction. Diffed the only commit
   that touched `manuscript.tex` in the past week (`939daef`, 2026-09-18) against its parent.
   Confirmed the Introduction (§1) is fully drafted (four paragraphs + roadmap) with nothing
   pending. Confirmed Section 2 ("Setup, identification, and algorithm") received only two small
   additions in that commit — a one-sentence forward-pointer in `sec:trees`, and the new
   `rem:saturated-general` remark — everything else in the commit is §3 work (hoisting
   `thm:regular`/`prop:sparse-bound`, relocating the single-tree family, adding
   `ass:pseudo`/`prop:pseudo-consistency`).
   - **Found a real gap:** `outline.md`'s own §2.2 plan commits to a new numbered assumption
     `ass:sparsity` (placed after `def:sufficient`, formally stating $\cS_e\ne\emptyset,
     \cS_\mu\ne\emptyset$, meant to collapse six unlabeled prose restatements scattered through
     §3 into one `\ref`-able assumption) — `outline.md`'s own status line claims the whole
     restructuring pass was "**EXECUTED**." It was not: grepped the current manuscript, `ass:sparsity`
     does not exist as a label anywhere. This is exactly the outline-says-X/manuscript-says-not-X
     divergence the project's registries exist to catch, and it did not get caught until this
     session.

**Decisions.** Discussion edits kept as a standalone unit (author's explicit direction), separate
from the still-open question of what to do about `ass:sparsity`.

**Open questions.** Whether to draft `ass:sparsity` into §2.2 now (as the most likely candidate
for "on to Section 2") — asked the author, not yet answered as of this entry. LaTeX compilation
of the Discussion edits has not been verified (no `compile-latex`/`verifier` pass run this
session).

**Next.** Resolve the `ass:sparsity` question with the author; if confirmed, draft it
paragraph-by-paragraph as the actual Section 2 continuation, same audit discipline as above.

---

## 11:40 — section-2.1-audit-and-fix

**Goal.** Author redirected to §2.1 ("Data, estimand, and notation") specifically, asking for an
audit first, edits after. Commissioned `domain-reviewer` (session `ses_f3b984b7dffewvRdgT84DXew80`)
and `proofreader` (session `ses_f3b984b3bffeeKqrDS9Ly48kjW`) against §2.1 and everything downstream
that depends on it, then applied the findings once the author approved the list.

**What happened.**

1. **The lead going in was wrong, in an instructive way.** Suspected `cor:variance`'s proof used
   an unstated hypothesis ("uniformly bounded conditional second moment of $Y$"). `domain-reviewer`
   traced the full hypothesis chain (`cor:variance → prop:P-instantiation1 → lem:uniform →
   lem:selection`) and found the condition **is** stated, verbatim, in `lem:selection`'s own
   hypothesis list — the apparent gap came from `notation.md`'s own stale line references pointing
   at the wrong location. No manuscript defect; a registry defect instead.
2. **One real MAJOR finding surfaced anyway:** `cor:variance`'s proof had an unjustified
   same-data LLN interchange (asserting $\hat V(\tau_e,\tau_\mu)\to_p V$ via a plain LLN when the
   summands depend on $n$ through the fixed-partition refits) — the identical pattern a
   `proof-auditor` pass had already caught and required fixing elsewhere in this manuscript
   (`prop:pseudo-consistency`). Fixed with one added mechanism clause (`ass:construct` bounds
   $\hat w$ by $(1-c)/c$, so the LLN applies leaf by leaf over finitely many bounded plug-ins).
3. **Several MINOR findings, all fixed:** `cor:width` cited hypotheses by description instead of
   `\ref`; $\to_p$ was used six times downstream but never defined (asymmetric with
   $\rightsquigarrow$, defined in the same sentence); $\cY$ was dead weight (introduced, never used
   again — confirmed by grep, not just inferred); $m_1(\bx)$ was *worse* than `notation.md`
   previously claimed (not "used only in prose" — used nowhere at all); the identified-ATT display
   had no label, so it got restated longhand at `thm:regular` instead of `\eqref`'d; bold/unbold
   notation drift on $O$/$\bO$ and $X$/$\bX$ at three specific sites (§2.1's own observation list,
   `eq:score`, `ass:causal`'s ignorability clause) against the manuscript's dominant bold
   convention; one concrete instance of the already-known "$w$" collision (propensity odds vs.
   control-weighted norm subscript) that could invert a reader's understanding of
   `lem:biasbound`'s norm. Proofreading fixes (missing commas, trailing whitespace, an awkward
   "draw from the random variable" phrasing, a mid-sentence verb switch) bundled into the same
   paragraph rewrites.
4. **Confirmed as NOT gaps** (checked and left alone): the two-step ATT-via-potential-outcomes →
   identified-form exposition; $Y\in\R$ with no boundedness claim (boundedness is a deliberately
   graded, locally-scoped condition, not a standing property); the iid-in-prose-then-re-declared-
   as-hypothesis pattern.
5. **Explicitly out of scope, flagged not fixed:** a separate, more pervasive lowercase
   $x$-vs-$\bx$ inconsistency throughout the document (e.g. `ass:causal`'s own positivity display)
   — broader than what was audited and approved this pass; a full re-anchoring of `notation.md`/
   `claims.md`'s stale line numbers (better done once drafting settles than repeatedly mid-session).

Updated `notation.md` (rows for $\theta_0$, $\bO$, $Y\in\R$, $m_1$, $D_w/D_\mu$ — including fixing
a nonexistent `\Lw` macro borrowed from `theory.tex` that would not compile if pasted into
`manuscript.tex` — and a new row for $\to_p$). Re-verified label/`\ref` integrity after every edit
(45 labels, up from 44 for the new `eq:att-id` label; 36 refs; zero missing, zero duplicates). Full
rationale in `quality_reports/reviews/2026-09-21_section2.1-audit.md`.

**Decisions.** None beyond executing the approved fix list; no structural/story-level changes this
pass.

**Open questions carried forward.** `ass:sparsity` (from the 09:05 entry) is still unresolved.
LaTeX compilation has not been verified for either of today's two edit passes (Discussion, §2.1) —
needs a `compile-latex`/`verifier` run before either is treated as fully final.

**Next.** Author to review both edit passes. Resolve `ass:sparsity`; consider whether to fix the
broader lowercase $x$/$\bx$ drift and the registry line-number staleness flagged but deferred.

---

## 13:15 — ate-appendix-added

**Goal.** Author requested: add ATE theory to the (previously empty) Appendix, and state right
after introducing the ATT estimand that ATE theory is available there.

**What happened.**

1. **Scoped before drafting.** Flagged two real issues before writing anything: (a) this directly
   conflicts with the Discussion paragraph drafted earlier today, which explicitly says the ATE
   extension "is future work" and "we... make no claim to that effect" — adding real content
   there requires updating that paragraph to match, not just appending; (b) the ATE's own
   identification needs *both* outcome regressions (not just $\mu_0$, as the ATT does), so the
   natural doubletree analogue is **three trees**, a real structural point, not a technicality.
   `theory.tex` confirmed to have zero existing ATE content — this is genuinely new derivation,
   not a lift from the companion document. Asked the author to pick a scope (minimal sketch / full
   proved parallel treatment / bare identification only) given the large effort spread between
   them; author chose **minimal: estimand + identification + sketch**.
2. **Drafted** a new `\appendix` section (`app:ate`): the ATE estimand, its identification
   (`eq:ate-id`, via both outcome regressions, no propensity), the efficient influence function
   (`eq:score-ate`, standard AIPW form, citing `hahn1998`), and one Remark (`rem:bilinear-ate`)
   deriving the bilinear bias decomposition by direct calculation — explicitly NOT a proven
   selection-consistency/CLT/anchor-interval extension, by design (no numbered
   proposition/theorem/corollary in the appendix, only equations and one hedged remark). Added the
   requested pointer sentence in §2.1 right after `eq:att`. Updated the Discussion's existing
   ATT-vs-ATE paragraph to reference the appendix instead of pure "future work," without
   overclaiming what the appendix actually proves.
3. **Commissioned `domain-reviewer`** (session `ses_f3b7ba358ffeIY7ZRtqC77Ai4c`) to independently
   re-derive every displayed result from scratch, and `proofreader` (session
   `ses_f3b7ba2a8ffeGDnfLLyjvMTGBR`). **Headline result: no mathematical error found** — the
   identification result, the EIF (all four signs, mean-zero at the truth), and the bilinear bias
   identity (the riskiest derivation, including the control-arm sign, which does not flip) all
   independently verified correct. The reviewer additionally surfaced a genuinely new,
   non-obvious internal-consistency fact: the ATE's control-arm bias term is *exactly*
   `prop:bilinear`'s own ATT remainder up to the $\pi^{-1}$ normalization — now stated in the
   remark.
4. **Three MAJOR defects found and fixed anyway** (all in the prose framing, none in the algebra):
   a false claim that the ATT's own identification result "involves the propensity" when it does
   not (contradicts `thm:regular`'s own wording); conflating identification-level positivity
   (needs only pointwise two-sided) with efficiency-level positivity (needs the uniform bound) —
   exactly the distinction the main text is already careful about for the ATT's one-sided bound,
   which this appendix's first draft fell below; and a missing hypothesis on the candidate
   propensity in the bilinear-bias remark's displayed identity (needed for the expectations to be
   finite). Several MINOR fixes bundled in (wrong section cited for "structural sparsity," missing
   citation on the EIF, symbol reuse — $c$ for two different constants, $m$ silently overloading
   the pre-existing leaf-floor $m_n$ — a near-colliding label renamed, plus proofreading polish).
5. Updated `outline.md` (Appendix row), `claims.md` (ATT-vs-ATE scope row + three new rows for the
   appendix's new objects), `notation.md` (new Appendix-local symbol block, explicitly verified not
   to collide with the main text's $\theta_0$/$\eta$/$\psi$/$c$/$m$ meanings). Full rationale in
   `quality_reports/reviews/2026-09-21_ate-appendix.md`. Re-verified label/`\ref` integrity after
   every edit (49 labels, up from 45; 38 refs; zero missing, zero duplicates).

**Decisions.** Minimal-sketch scope (author's explicit choice, not assumed). The appendix
deliberately contains zero numbered proposition/theorem/corollary — only labeled equations and one
hedged remark — to keep the proof-strength claims honest at exactly the level authorized.

**Open questions carried forward.** Same three as the day's earlier entries: `ass:sparsity`
unresolved; LaTeX compilation unverified for all three of today's edit passes; the broader
lowercase $x$/$\bx$ notation drift and registry line-number staleness both flagged, both deferred.
Additionally: whether a propensity-free single-tree analogue exists for the ATE (raised, not
answered, by both the appendix and the Discussion paragraph) is now an explicit open question in
the manuscript itself, not just a session note.

**Next.** Author to review this pass alongside the other two. A `compile-latex`/`verifier` run is
overdue across all of today's edits before any is treated as fully final.

---

## 16:40 — real-data-application-server-prep

**Goal.** Author asked to try running some real-data analysis. Confirmed live (not from stale
docs) that this isn't possible from this environment, then prepped for the author's own upcoming
server session instead.

**What happened.**

1. **Ran the pipeline live to confirm current blocking state**, rather than trusting last week's
   README: `application/run_tests.R` still 99/0/0. `01_declare_requirements.R` confirms this
   environment has no `smi_census()` record for anything — real data is genuinely unreachable
   here, not a config issue. `02_population_and_eligibility.R` runs clean. `07_estimate_att.R`
   aborts on exactly 2 of the original 4 `OPEN_DECISIONS` (`enrollment_source`,
   `diagnostics`) — `cost_family_scope` and `msr_code` are resolved.
2. **Traced `enrollment_source` to its actual gap**: the enrollment table
   (`larger_smi_medicaid_monthly_flag`) is identified and confirmed to exist, but no threshold
   exists for "enough" months of `MEDICAID_FLAG=1` coverage across the 24-month post-index window
   to count as complete-case, and the `TYPE` column's role is unchecked. Found smidata's own
   `SERVER_SESSION_RUNBOOK.md`, which independently confirms the same priority ordering
   (resolve this, then `diagnostics`, then run `01_census.R`) and warns explicitly: budget **one**
   hours-scale, file-touching server operation per session (`.sas7bdat` file-open cost is hours
   regardless of bytes subsequently read) — census and a `medicaid_monthly_flag` deep-dive are
   each their own hours-scale commitment and probably don't both fit.
3. **Author chose to prep for resolving both open decisions live** (author can decide the
   enrollment threshold once they see the real distribution, and settle `diagnostics` on the
   spot). Wrote `application/server/enrollment_coverage.R` — a new evidence-gathering script (no
   such script existed anywhere before this session) that reads
   `larger_smi_medicaid_monthly_flag` (24M rows, 5.8 GB — confirmed schema from the existing
   2026-09-15 fingerprint: `ID, INDEX_DT, TYPE, COHORT, YEAR_MONTH, MEDICAID_FLAG`, no need to
   guess) and reports, as suppressed/aggregate summaries only: what `TYPE` and `COHORT` actually
   partition, a uniqueness check on `(ID, YEAR_MONTH, TYPE)`, the real distribution of
   months-covered out of 24 per patient, and coverage broken out by `TYPE`. Explicitly does NOT
   pick a threshold — states plainly this is evidence for a PI/author call, not a resolution.
   Reuses established `smidata` conventions throughout (`smi_read()`, `lubridate::add_with_
   rollback()` matching `smi_patient_windows()`'s own month arithmetic, abort-rather-than-guess
   parsing matching `smi_parse_msr_yr()`'s own philosophy, `smi_export()`'s small-cell threshold).
4. **Could not place the script in `smidata` itself**, where it belongs (parallel to
   `smidata`'s own `05_cohort_overlap.R` — same role, evidence for a cross-project open decision,
   not a per-consumer duplicate) — this session's sandboxed workspace has no write access to
   `~/RAND/tools/smidata` (confirmed via a failed `touch`). Placed in `doubletree/application/
   server/` instead, with an explicit header noting it should be promoted into `smidata`'s
   `inst/server/` (as `07_enrollment_coverage.R`, next after `06_ingest.R`) once the author has
   write access there, since resolving `enrollment_source` here should resolve it for
   dual-bounds too, per the registry's own `registry_ref` convention — that only works cleanly
   from the shared package, not a per-project copy.
5. **Verified what could be verified without server access**: R syntax-parsed clean; the core
   computational logic (Sections 2-4) dry-run against a hand-built synthetic dataset matching the
   real 6-column schema, with sanity checks (`months_covered <= 24`, `months_present <= 24` for
   every synthetic patient) passing.

**Decisions.** Real analysis needs the author's own server session — nothing further to do from
this environment until that happens.

**Open questions carried forward, all from earlier today plus:** whether `diagnostics` gets
resolved in the same server session (it's a pure conversation, not a data question — the new
script doesn't touch it at all) or scheduled separately.

**Next.** Author runs `application/server/enrollment_coverage.R` on the actual server (after
`smidata`, `haven` install per `SERVER_SESSION_RUNBOOK.md` steps 1-2 if not already present),
decides the `enrollment_source` threshold from the real distribution, records it in both
`application/_config.R` and dual-bounds' equivalent entry, and settles `diagnostics`. Once both
close, `07_estimate_att.R` should run for a real number for the first time.

---

## 12:40 — session close

**Compiled `manuscript.tex` for the first time this session** (never done across any of the
three earlier edit passes) — 4 XeLaTeX passes + bibtex, fully converged on pass 4: zero undefined
citations, zero undefined references, zero multiply-defined labels, 27 pages stable. 5 overfull
hboxes, all minor/cosmetic, the largest (31.8pt) in `sec:lasso-comparison`, content untouched this
session — not investigated further, not blocking.

**Committed** (`896a405`) — no push: this sandboxed session has no SSH access to `origin`
(confirmed via a failed `git fetch`), matching the reason for the planned move to an
un-sandboxed session next. Scoped to exactly this session's own files, following the same
discipline the 2026-09-17 session's own close-out entry established: `inst/paper/{claims,
manuscript,notation,outline}.{md,tex}`, `application/server/enrollment_coverage.R`,
`quality_reports/reviews/2026-09-21_*.md` (all three), `session_notes/2026-09-21.md`.

**Deliberately excluded**, confirmed pre-existing and not mine via `git diff`/mtime before
staging (same pattern as 2026-09-17's own exclusions): `quality_reports/plans/
2026-09-15_real-data-pipeline-spec-shell.md` and `session_notes/2026-09-17.md` (both modified,
neither touched by me — mtimes 2026-09-16/18); `.omo/`, `inst/paper/manuscript-bak.tex`,
`inst/paper/quality_reports/session_logs/`, `quality_reports/plans/
2026-09-15_real-data-pipeline-spec.md`, and both `simulations/*/tables/` directories (all
untracked, all pre-existing).

**Next session (push + real work):** push `896a405` to `origin` once SSH access is available;
resolve `ass:sparsity` (carried from this morning, still open); the author's own server session
for `enrollment_coverage.R` / `01_census.R` / the `diagnostics` conversation.





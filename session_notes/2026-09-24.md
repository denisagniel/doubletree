# Session Notes — 2026-09-24

## 07:57 — Tier-0 unblocked via smidata's v0.3.0 census; optimaltrees.so found blocked (unrelated)

**Goal.** smidata shipped its first real census snapshot today (v0.3.0,
`2026-09-21_2824080e`). Check whether this pipeline's Tier-0 fixture gap (same
census gap as dual-bounds', which this design reuses in full) is actually
closed, and fix anything that needs fixing.

**What happened.**

- Reinstalled `smidata` locally, ran `application/01_declare_requirements.R`.
  Same bug as dual-bounds' identical script: hardcoded "Tier-0 is BLOCKED: 0 of
  38 ... have no smi_census record" — self-contradicting once the count is
  actually 0. Confirmed via `smi_brief()` that all 66 datasets in the snapshot
  carry `census = TRUE`.
- Fixed `01_declare_requirements.R`: added the missing branch so it reports
  "Tier-0 is UNBLOCKED" when nothing is actually missing. Re-ran to confirm.
- Updated `enrollment_source`'s note (this pipeline's own complete-case-status
  question, same underlying table as dual-bounds' `enrollment_source`) with
  the census's real finding: `larger_smi_medicaid_monthly_flag$TYPE` has
  exactly 2 values (`IP`, `OP`, complete). Resolves "the TYPE column's role is
  unchecked" in the "needs a filter" direction. Did not invent which of IP/OP,
  or the gap-months threshold — both remain open PI decisions.
- Ran `application/run_tests.R`: 75 passed, 3 skipped, but **1 real,
  unrelated error**: `estimate_att_crossfit()` -> `dyn.load()` on
  `optimaltrees.so` fails with `mmap(...) failed with errno=1`. Checked
  `codesign`/`spctl` on the file directly: ad-hoc signed, no real certificate,
  `spctl` reports `rejected`. This matches the signature of a security-tool
  block (same class as this morning's ThreatLocker `foo.so` incident in
  smidata, diagnosed the same session) rather than a corrupted build —
  `optimaltrees` compiles and installs fine, it's the *load* of the resulting
  `.so` into a running R process that's being intercepted. This blocks
  doubletree's own estimator from running locally at all, completely
  independent of census/ingest/anything data-related. Not fixed — needs the
  same IT/allowlist conversation as the `foo.so` incident, on a different
  binary.
- Committed the Tier-0 fix (`b02f63d`), pushed to `origin`
  (`github.com/denisagniel/doubletree`).

**Next.**
- Still blocking `06_assemble_analytic_data.R`/`07_estimate_att.R`:
  `enrollment_source` (gap-months threshold + which of IP/OP), `diagnostics`
  (PI explicitly wants a synchronous conversation on which of
  `estimate_att()`'s sparsity-proxy diagnostics gate a reportable estimate —
  deferred 2026-09-17, not resolved async).
- `optimaltrees.so`'s security-tool block needs resolving before
  `estimate_att_crossfit()` can run locally at all — separate from every other
  item above, and separate from anything smidata-side.

## 11:20 — Adversarial audit of S5 vs. its claims; loader fix, registry corrections, R4 anchor arm added (not yet re-run)

**Goal.** S5 (`head_to_head_comparison`) finished its full 37/37-cell sweep
2026-09-23. Before drafting the manuscript's planned §4 "Simulation evidence"
against it, adversarially audit whether the actual on-disk results support
what `claims.md` and the session notes already say they support.

**What happened.**

1. Re-derived every regime's numbers directly from the `.rds` checkpoints via
   `analyze.R::summarise_cell()`/`rmse_ratio_table()`, rather than trusting
   the 2026-09-23 prose summary. Four findings, three confirmed and refined
   (one corrected) via an `oracle` consultation
   (session `ses_f2c6c78b1ffelW5AfBYWwwKQMQ`). Full writeup:
   `quality_reports/reviews/2026-09-24_s5-claims-audit.md`.
2. **Stale-checkpoint contamination, fixed.** 19/37 result cells still had a
   2026-09-09 pilot/smoke-test checkpoint sitting next to the full-scale one;
   `load_results()` pooled both unconditionally (1,942 duplicate rows, 70
   genuinely non-identical, 64 of those in R4/DGP-C). Verified the numerical
   impact was <0.1pp everywhere before touching anything. Fixed
   `analyze.R::load_results()` to dedup per cell via a new
   `dedup_checkpoints()` (keeps the newest vintage, `cli::cli_abort()`s on any
   non-identical collision instead of silently pooling); verified it
   correctly aborted against the still-contaminated directory, then archived
   the 34 stale `_r2.rds`/`_r30.rds` files to
   `simulations/head_to_head_comparison/results/archive/pilot_2026-09-09_superseded/`
   (not deleted; `results/` is gitignored, so nothing here touches git).
   `results/` now holds exactly 37 `cell_*.rds` files. Re-verified
   `load_results()` reproduces the hand-checked "clean" numbers exactly.
3. **`claims.md` corrections.** `rem:two-trees-why`'s single "Covered" row
   split into four sub-claims (two population-identity pieces correctly
   "Not sim-testable as stated," one new falsifiable GAP for an
   untested non-tree-propensity arm, one theoretical citation) — the old row
   cited S5's C3 for a proof-*mechanism* claim, which
   `head_to_head_comparison/README.md` §0's own licensing sentence
   ("no comparative sentence beyond 'matched X'...") does not actually
   license. `ass:causal`'s row reworded: R4's coverage shortfall
   (89.7%→93.7%, n=500→2000, vs. 95% nominal) is real and only
   partially resolving, but it's a **small-c** regime (c>0 throughout DGP-C),
   not an `ass:causal` violation as the auditor's first draft claimed —
   `oracle` corrected this. Table 2's `head_to_head_comparison` row updated
   with the sweep's actual completion status, the emergency `optimaltrees`
   tie-breaking-revert caveat (S5 ran under a pre-`140d110` revert because the
   intended deterministic `single_model()` extraction has a live,
   unresolved glibc heap-corruption bug — see 2026-09-23's notes), and a
   pointer to the stale-checkpoint fix.
4. **R4 anchor arm added, not yet run.** `common.R`'s R4 regime definition
   now includes `doubletree_crossfit` + `doubletree_anchor`, with a
   pre-registered, falsifiable prediction recorded in-line (anchor widens but
   still falls short of nominal, because it targets bias and R4's shortfall
   is variance/tail-driven near the thin propensity margin — if this
   prediction is wrong, that's itself a finding, not something to absorb
   quietly). Confirmed the harness's derived-arm machinery
   (`DERIVED_ARMS`/`run_one_rep()`) is fully regime-agnostic, so no other code
   needed to change. Both files parse clean (`parse()` on `common.R` and
   `analyze.R`).
5. **Could not execute the R4 re-run.** Confirmed (again, more precisely than
   the 07:57 entry above) that `optimaltrees` cannot currently be loaded
   locally by either path: dev-mode `pkgload::load_all()` fails to compile
   (missing build tools), and the already-installed binary fails at
   `dyn.load()` with `mmap(...) errno=1` — same security-tool-block signature
   as the unrelated `.so` blocked this morning, on a different file. This
   needs the O2 cluster. **Gotcha recorded for whoever runs it:**
   `run_sweep.R`'s resumability check is keyed only on
   `file.exists()` per `(regime, dgp, n)` cell — it has no way to detect that
   a cell's *arm set* changed, so resubmitting R4 as-is will silently skip
   all three cells unless the existing
   `cell_R4_C_n{500,1000,2000}_r1000.rds` are moved aside first. Exact
   command in `quality_reports/reviews/2026-09-24_s5-claims-audit.md`.

**Decisions.**
- Ideally the R4 re-run happens *after* the upstream `single_model()` bug is
  fixed, not under the current emergency revert — R4/DGP-C is the cell most
  exposed to that revert's tie-breaking change (64/70 of the audit's
  mismatched rows), so re-running the new anchor arm under the same revert
  would just inherit the same exposure the flagship arm already has there.
- Archived, not deleted, the stale pilot checkpoints — consistent with this
  project's general no-silent-deletion posture, and cheap to reverse if
  wrong.

**Mistake worth recording.** The auditor's first draft of the R4 finding
called it an "`ass:causal` violation." `oracle` corrected this: DGP-C's worst
$e_0=0.982<1$, so $c>0$ holds throughout — it's a small-$c$ regime, not a
positivity failure, and the reason the anchor doesn't help is that its
widening mechanism targets bias, not the variance/tail failure R4 actually
exhibits (relative bias shrinks fast while undercoverage persists — a
dissociation, not a coincidence). Framing this correctly changes what the
manuscript needs to say about it (empirical confirmation of an already-
disclosed asymptotic-vs-finite-sample caveat, not an undisclosed gap) and
was worth the extra consultation round before writing anything into
`claims.md`.

**Open questions / next.**
- Run the R4 anchor+crossfit re-run on O2 once the toolchain block is cleared
  there (it already loads fine on O2 per the 2026-09-23 sweep) — ideally
  after `single_model()`'s bug is fixed, per the decision above.
- `glm_main`'s R1 relative bias plateaus at 25–30% through $n=4000$ and never
  approaches the closed-form asymptotic 45% prediction the pilot deferred
  resolving to the full sweep. Not investigated this pass; flagged in the
  audit's "not yet done" list.
- R2's two-factor confound (splitting *and* `max_depth`) limits what C3 can
  ever license about *why* not-splitting works, independent of today's
  registry fix. A matched-nuisance-class R2 variant would close this
  properly; not implemented.
- The local `optimaltrees.so` security-tool block (both today's application-
  pipeline instance and this session's) is the same unresolved IT-level issue
  — still needs an allowlist/certificate conversation, not a code fix.

## 12:39 — PI resolves enrollment_source (fully); diagnostics (partially); CHECK 4 added

**Goal.** Encode today's PI answers on the two blockers left over from the
07:57 entry above (`enrollment_source`'s gap-months threshold + IP/OP filter,
`diagnostics`) before they're lost to a future async gap — dual-bounds
received the identical `enrollment_source` answer in the same sitting per
its own registry.

**What happened.**

- `OPEN_DECISIONS$enrollment_source` in `application/_config.R` moved from
  `open_decision`/`blocking_final = TRUE` to `confirmed`/`blocking_final =
  FALSE`. PI gave two separate answers: (1) TYPE (IP/OP) — "note this as a
  question and just proceed ignoring this for now," so complete-case status
  is computed from `larger_smi_medicaid_monthly_flag` without filtering on
  TYPE; (2) gap-months — "let's just not have a gap for now. We can change
  this later," so complete-case status now requires full continuity (0-month
  tolerance, `MEDICAID_FLAG=1` for every one of the ~24 months in
  `[INDEX_DT, INDEX_DT+24mo]`). Both are recorded as explicit, PI-given,
  revisable defaults — not implementation guesses — with a `registry_ref` to
  dual-bounds' identical resolution.
- PI separately attached a prerequisite to the TYPE answer: verify no patient
  carries conflicting TYPE values before trusting "ignore TYPE" is neutral.
  Added CHECK 4 to `application/90_checks_tier1.R` (identical to
  dual-bounds' CHECK 3 — same table, same question, run once for both
  papers): per `(ID, YEAR_MONTH)`, how many patients carry both IP and OP
  rows, and within those, whether `MEDICAID_FLAG` ever disagrees between the
  two. Disagreement would mean "ignore TYPE" silently picks whichever row is
  read first/last rather than being a no-op. **Server-only, not yet run.**
- `OPEN_DECISIONS$diagnostics` moved to "partially resolved": the *general*
  diagnostics standard for the paper (overlap/balance checks plus nuisance
  fit vs. OLS/GLM competitors) is now `confirmed` in smidata's
  `doubletree__application.yml` registry. That does **not** resolve this
  entry's narrower question — which of `estimate_att()`'s own sparsity-proxy
  outputs (`certified_e`, `gap_e`, etc.), at which thresholds, license
  reporting the flagship estimate over the cross-fit fallback. Still
  `open_decision`/`blocking_final = TRUE`; note explicitly says not to invent
  a threshold, since the OLS/GLM comparison may inform one but doesn't exist
  yet.
- Changes made only to `application/_config.R` and `application/
  90_checks_tier1.R`; not yet committed as of this note.

**Next.**
- `05_complete_case.R` is no longer blocked on missing enrollment data — it
  can now be implemented against `type_filter = "none"`, `gap_months = 0L`
  instead of remaining a stub. This is the concrete unblocked next step.
- Run CHECK 4 on the server before leaning on the "ignore TYPE" default for
  any reported number — it's a cheap check that could overturn the interim
  answer if TYPE turns out not to be neutral.
- `diagnostics`' proxy-threshold sub-question is still open and still blocks
  `07_estimate_att.R`'s final reporting logic (which of flagship vs.
  cross-fit gets reported). Unaffected by today's resolution.

## 13:17 — workspace cleanup; implemented 05_complete_case.R for real

**Goal.** User: "clean everything up, then work on the 05 step in the
pipeline." Two parts.

**Cleanup.** Classified every dirty/untracked path before touching anything —
real unlanded work is not clutter. Committed (not archived):
`quality_reports/plans/2026-09-15_real-data-pipeline-spec.md` (the actual
PI-confirmed spec, previously untracked entirely) and a restored
`session_notes/2026-09-17.md` entry that appears to have been dropped in an
earlier merge (verified against `88ba1bd`'s real scope before restoring, not
fabricated). Archived (`archive/cleanup-2026-09-24-124548/`, manifest inside,
nothing deleted): a stale `manuscript-bak.tex`, 20 pilot-vintage
(2026-09-09) `simulations/*/tables/*.csv` files superseded by the same-day S5
audit's `.rds`-side cleanup, and an empty `session_logs/` scaffold. Added
`.omo/` and `simulations/*/tables/` to `.gitignore`. Three commits: `b86b953`
(this morning's PI-resolution doc/commit, done first), `6af4dfd` (landed the
stray work), `7cb8741` (the archive).

**05_complete_case.R: real implementation, not a stub.**

1. Before writing anything: found `larger_smi_medicaid_monthly_flag` was
   never registered in `SMI_KEYS`/`SMI_COLS`/`01_declare_requirements.R` --
   a real gap, separate from the stub itself, even though this morning's
   `enrollment_source` resolution already named the table. Fixed as part of
   this pass: added the key/columns/`smi_require()` call (39 requirements
   now, was 38).
2. Explored in parallel: dual-bounds' analogous script (also still
   pseudocode-only -- no executable prior art there either) and smidata's
   `smi_*()` surface (no helper covers a monthly-flag-grain table; both
   `smi_patient_windows()`/`smi_window_coverage()` operate on span tables).
   Found the real prior art: smidata's own unexported evidence-gathering
   script, `inst/server/07_enrollment_coverage.R` -- already computes almost
   this exact metric against the same table, for the PI's original decision.
3. Consulted `oracle` (session `ses_f2b74283bffeBrBtfuTiXlBKB7`) with that
   script's logic plus five concrete design questions before writing code:
   missing-patient semantics, `INDEX_DT` source of truth, `YEAR_MONTH`
   parsing placement, pure-helper architecture, and whether to gate on
   CHECK 4 (not yet run). Oracle's answers, all adopted: make the helper
   **total** (one row per cohort patient, explicit `FALSE`, never an
   implicit `NA`); anchor the window on `cohort$INDEX_DT` (matching `06`'s
   own usage), cross-check the table's own copy and **abort** on
   disagreement rather than the evidence script's lenient `min()`; verify
   coverage by **month-set membership**, not a count of 24 (the evidence
   script's `INDEX_DT`-not-floored-first pattern can silently shift the
   window by a month and certify a patient uncovered at index as complete);
   don't gate on CHECK 4 -- self-verify the identical question at
   cohort/window-restricted scope instead, and abort if it ever disagrees.
4. Implemented `application/helpers/complete_case.R` (pure, no data reads, no
   `smidata` dependency -- matches `discretize_prior_cost.R`'s convention):
   `parse_year_month()` (runtime format detection + abort, checked against
   every value, not a sample -- mirrors `07_enrollment_coverage.R`'s
   Section 3 but stricter, since every row here can flip an inclusion
   decision), `ym_month_index()` (integer month arithmetic, sidesteps
   `Date`/rollback entirely), `compute_complete_case()` (the full function,
   with the ID-join-key-class guard, the `INDEX_DT` cross-check, the TYPE
   self-check that aborts on disagreement, and the all-absent sentinel
   Oracle flagged as the likeliest silent failure mode).
5. Rewrote `05_complete_case.R`'s header (the old one asserted complete-case
   status was unanswerable -- no longer true) and orchestration: the real
   `smi_require()`/`smi_read()` call sequence is written out, matching
   `02`/`06`/`07`'s "intended pipeline in comments" convention, because it
   needs `analysis_cohort` from a working `02` -- which does not exist
   today. **Found, while implementing this:** `02_population_and_eligibility.R`
   itself still hard-aborts on `msr_code`, even though that decision was
   confirmed 2026-09-17 -- its gate was never updated to match. Documented
   in the file header and README as a separate, pre-existing gap; not fixed
   here (out of scope for "work on 05").
6. Added `confirmed_value()` to `_config.R` alongside `open_value()`:
   `open_value()`'s "Unconfirmed value" warning is unconditional and would
   misleadingly fire on a `status = "confirmed"` decision (it already does,
   unfixed, in `02`'s existing `msr_code` read -- not touched). `05` uses
   the new accessor for `enrollment_source`.
7. Wrote `application/tests/test-complete-case.R`: 62 assertions, hand-
   calculated, covering the fully-covered/insufficient/absent cases, the
   mid-month-`INDEX_DT` shift bug (both showing it does NOT occur and
   showing a genuinely-missing index month is still caught), out-of-window
   rows not leaking into coverage, TYPE agreement vs. disagreement (and
   that a disagreement OUTSIDE the window doesn't wrongly abort), NA-flag
   handling, every input-validation abort, and an end-to-end run at the
   real 24-month `config_complete_case_months`.
8. One real bug caught by the tests, not by inspection: `dplyr::count()`
   produces no row at all for a patient with zero uncovered months, so
   after the `left_join` that became `NA` -- and the original code defaulted
   `NA -> window_months` (fully uncovered), exactly backwards for the common
   case. Fixed to default to `0` (fully covered); `is_present`, checked
   independently against the raw table, is what correctly flags a patient
   truly absent from `monthly_flag`. All 62 new assertions pass after the
   fix; `application/run_tests.R` is 135 passed / 1 failed / 3 skipped, the
   1 failure and 3 skips being the pre-existing, unrelated `optimaltrees.so`
   block (07:57 entry, above) -- not a regression.
9. Updated `application/README.md`: the `05_complete_case.R` stub row, the
   open-decisions table (three of four now confirmed, only `diagnostics`
   still blocks), dataset/assertion counts (38→39, 99→135), and flagged
   `02`'s stale gate in both the Files table and the Tier 2 status line.

**Next.**
- Run CHECK 4 on the server (still not done) -- `compute_complete_case()`'s
  self-check covers the cohort/window-restricted scope, not the whole-table
  question CHECK 4 answers.
- `02_population_and_eligibility.R`'s stale `msr_code` gate (found above) is
  the actual remaining blocker before `05`'s real orchestration can run
  against a real `analysis_cohort` -- separate task, not started.
- Differential attrition by treatment arm under the 24-month complete-case
  filter is a real, unaddressed threat (Oracle-flagged): retention plausibly
  correlates with the exposure itself. The commented orchestration in `05`
  sketches reporting it by arm; not run, since it needs real data.
- `diagnostics` remains the one open decision still `blocking_final = TRUE`.

